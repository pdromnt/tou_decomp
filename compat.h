#ifndef TOU_COMPAT_H
#define TOU_COMPAT_H

/* Keep the legacy DirectX API levels explicit before including its headers. */
#define DIRECTDRAW_VERSION 0x0100
#define DIRECTINPUT_VERSION 0x0700

#include <windows.h>
#include <ddraw.h>
#include <dinput.h>
#include <mmsystem.h>

#endif /* TOU_COMPAT_H */
